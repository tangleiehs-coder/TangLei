# TOOLS.md - Tool Configuration & Notes

> Document tool-specific configurations, gotchas, and credentials here.

---

## Tool Availability

Fill this in during setup so your agent knows which tools are available and which
fallbacks to use. The agent reads this file each session.

- [ ] `memory_search` — available / not available
- [ ] `session_status` — available / not available
- [ ] `cron` / scheduler — available / not available

---

## Credentials Location

> ⚠️ Never store actual credentials in this file.
> Keep them in `.credentials/` — auto-protected by `.gitignore`
> (generated by `python scripts/setup.py init`).

All credentials stored in `.credentials/` (gitignored):
- `example-api.txt` — Example API key

---

## [Tool Name]

**Status:** ✅ Working | ⚠️ Issues | ❌ Not configured

**Configuration:**
```
Key details about how this tool is configured
```

**Gotchas:**
- Things that don't work as expected
- Workarounds discovered

**Common Operations:**
```bash
# Example command
tool-name --common-flag
```

---

## Writing Preferences

[Document any preferences about writing style, voice, etc.]

---

## What Goes Here

- Tool configurations and settings
- Credential locations (not the credentials themselves!)
- Gotchas and workarounds discovered
- Common commands and patterns
- Integration notes

## Why Separate?

Skills define *how* tools work. This file is for *your* specifics — the stuff that's unique to your setup.

---

*Add whatever helps you do your job. This is your cheat sheet.*
